# Vigil SDK

A lightweight Python wrapper that captures LLM calls and tool uses from your AI agent.

## Usage
Coming in Week 2.

## What it captures
- Input prompt
- Output text
- Latency (ms)
- Token count
- Tool name, input, output (for tool calls)